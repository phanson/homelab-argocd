apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-storage
  namespace: booklore
  labels:
    app: booklore
spec:
  resources:
    requests:
      storage: 512Mi
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
