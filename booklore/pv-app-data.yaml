apiVersion: v1
kind: PersistentVolume
metadata:
  name: app-data
  namespace: booklore
  labels:
    app: booklore
spec:
  storageClassName: manual
  capacity:
    storage: 512Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/services/booklore/data
