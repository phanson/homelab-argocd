apiVersion: v1
kind: Service
metadata:
  name: rackula
  namespace: rackula
  labels:
    app: rackula
spec:
  selector:
    app: rackula
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
